/** @type {import('next').NextConfig} */
// next.config.js
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 'plus.unsplash.com'],
      },
};
  

export default nextConfig;
